# --- Core (unchanged from the original, known-compatible pins) ---
fastapi==0.110.0
uvicorn==0.28.0
jinja2==3.1.3
python-multipart==0.0.9
requests==2.31.0
beautifulsoup4==4.12.3
markdown==3.5.2
rich==13.7.0
pydantic>=2.0.0

# --- Added for the SaaS rewrite ---
python-dotenv>=1.0.0     # loads .env locally so you don't have to `export` keys by hand
itsdangerous>=2.1.0      # signs the anonymous per-browser session cookie (utils/session.py)
bleach>=6.1.0            # sanitises LLM-generated HTML before it reaches the browser (utils/security.py)
slowapi>=0.1.9           # per-IP rate limiting on the research/chat endpoints (app.py)
pypdf>=4.0.0             # extracts text from uploaded PDFs (app.py /api/upload-report)
edge-tts>=6.1.0          # free, natural-sounding podcast voices via Microsoft Edge (no API key needed)

# NOTE: these version pins are known-compatible as of when this file was written.
# It's good practice to periodically run `pip install -U -r requirements.txt` and
# re-test, rather than staying pinned forever.
